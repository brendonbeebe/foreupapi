  language: node_js
  node_js:
  - 0.10
  before_install:
  - chmod a+x ./scripts/test
  - npm install -g dredd
  script: ./scripts/test
  notifications:
    email:
      recipients:
        - brendon@foreup.com
      on_success: always # default: change
      on_failure: always # default: always
